export const checkFeatureCombination = (features) => {
    // Custom logic to validate feature combinations
    // For example, certain features may not be compatible together
    let isValid = true;
    // Add validation logic here
    return isValid;
};
